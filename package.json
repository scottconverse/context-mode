{
  "name": "context-mode",
  "version": "1.3.1",
  "type": "module",
  "description": "Context window optimization for Cowork. Sandboxes tool output, compresses what returns with a self-learning 3-stage pipeline, and tracks session state — reducing context consumption by up to 98% while preserving errors and session-relevant content verbatim.",
  "main": "server/index.js",
  "bin": {
    "context-mode": "install.js"
  },
  "scripts": {
    "start": "node server/index.js",
    "test": "node test-e2e.js && node test-adversarial.js",
    "setup": "node scripts/setup.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "better-sqlite3": "^12.6.2",
    "zod": "^3.25.0"
  },
  "optionalDependencies": {
    "turndown": "^7.2.0",
    "turndown-plugin-gfm": "^1.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "SEE LICENSE IN LICENSE",
  "devDependencies": {
    "md-to-pdf": "^5.2.5"
  }
}
